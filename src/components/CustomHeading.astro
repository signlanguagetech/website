---

interface Props {
  class?: string;
}

const { class: className } = Astro.props;
---

<h2 class={`custom-heading ${className}`}>
  <slot />
</h2>

<style>
  .custom-heading {
    margin-top: 100px;
  }
</style>
